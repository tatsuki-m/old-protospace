$('#comments_number').text('(<%= @prototype.comments.size %>)');
$('#comment-field').val('');
$('#comments').html(
  '<%= j render partial: 'prototypes/comment', collection: @prototype.comments  %>'
);
